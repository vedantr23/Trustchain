# Trustchain

A platform for transparent and secure charity donations using blockchain-inspired tracking and security.

## Project Structure

- `frontend/`: React application (Vite + TypeScript)
- `backend/`: Node.js server with Supabase integration

## Setup

1. **Backend**:
   - Navigate to `backend/`
   - Run `npm install`
   - Set up your `.env` file with Supabase credentials.
   - **Database Setup**: Open the Supabase SQL Editor and paste the contents of `backend/schema.sql` to initialize the optimized schema and RLS policies.

2. **Frontend**:
   - Navigate to `frontend/web/`
   - Run `npm install`
   - Run `npm run dev` to start the development server.

## Collaboration

This project is open for collaboration. Please ensure you create a branch for your features and submit a PR.
